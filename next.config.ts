import type { NextConfig } from "next";
import { BASE_PATH } from "./lib/base-path";

const nextConfig: NextConfig = {
  /* config options here */
  basePath: '/client-websites/cheers'
};

export default nextConfig;
